#include <iostream>
#include <vector>

// write a function that returns a vector of
// the first three multiples of num is ascending order
// Define first_three_multiples() here:


int main() {
  
  for (int element : first_three_multiples(8)) {
    std::cout << element << "\n";
  }
  
}